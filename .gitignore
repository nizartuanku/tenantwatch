# The compiled binary at the repo root. The trailing "!" line keeps the
# tenantwatch/ PACKAGE directory tracked — without it git silently ignores every
# new file added there, and the repo stops compiling for anyone who clones it.
/tenantwatch
!/tenantwatch/
*.db
tenants.json
*-license.key
/dist
